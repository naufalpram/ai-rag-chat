import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Ask EDTS Knowledge",
  description: "An AI-powered Retrieval-Augmented Generation (RAG) chat assistant for EDTS, designed to help employees quickly find accurate, up-to-date internal company information from a secure knowledge base.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
